# 複製成 .env.local 後填入自己的金鑰（.env.local 不會被 commit）
# 申請：https://carto.com/basemaps/apikey/
VUE_APP_CARTO_API_KEY=
# 金鑰綁定的網域（逗號分隔）。留空則使用預設 wenlab501.github.io
# 非此清單中的來源（例如 localhost）不會帶金鑰，底圖會有浮水印但仍可顯示
VUE_APP_CARTO_KEY_HOSTS=
